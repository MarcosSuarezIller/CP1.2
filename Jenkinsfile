pipeline {
    agent any

    stages {
        stage('Test Jenkins') {
            steps {
                echo 'Jenkins está funcionando'
                bat 'python --version'
            }
        }
    }
}